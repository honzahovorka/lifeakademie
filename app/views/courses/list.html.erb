<section class="sixteen columns">
  <table class="custom-table">
    <thead>
      <tr>
        <th>Název</th>
        <th>Město</th>
        <th>Začátek</th>
        <th>Cena</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @courses.each do |course| %>
      <tr>
        <td><%= link_to course.name, admin_course_path(course), title: 'Informace o kurzu' %></td>
        <td><%= course.city %></td>
        <td><%= I18n.localize(course.start_date) %></td>
        <td><%= number_to_currency course.price %></td>
        <td class="c"><%= link_to 'Zobrazit', '#' %></td>
        <td class="c"><%= link_to 'Upravit',  '#' %></td>
        <td class="c"><%= link_to 'Smazat',   '#' %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</section>
