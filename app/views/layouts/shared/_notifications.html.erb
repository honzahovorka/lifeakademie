<!-- Notifications -->
<div class="container">
  <div class="sixteen columns">
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: "#{name} flash_notification hidden" %>
    <% end %>
  </div>
</div>
